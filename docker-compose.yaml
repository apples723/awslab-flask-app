version: "3.7"
services:
  caddy:
    image: apples723/awslab-flask-app:v1.0.0
    container_name: awslab-flask
    ports:
      - 8000:8000
    restart: always
networks:
  default:
    external:
      name: caddy